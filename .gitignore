
# nix build result symlinks (any name)
*-result
result
result-*

# local .axp copies (1.4 GB firmware bundles)
*.axp

# python bytecode (py_compile checks on the display daemon)
__pycache__/

# per-machine Claude Code overrides
.claude/settings.local.json

# js deps (web/ fork; pnpm install for lint/typecheck runs)
node_modules/
